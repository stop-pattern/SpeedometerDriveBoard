#include <Arduino.h>
#include "define.h"


void DACout(int num, byte ch) {
  //dh = (ch) ? ch : ch;
  //SPI.send(num, ch);
}

void Shiftout(byte num) {
  for (int i = 0; i >= 6; i++) {
    //shiftOut(0, 0);
  }
}
